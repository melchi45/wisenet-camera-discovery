# TC — Window UI Full Specification & Reimplementation

| | |
|---|---|
| Title | Window UI Full Specification & Reimplementation — Test Cases (TC) |
| Abstract | Playwright equivalence test cases (old `src/shared/` vs. new `src/shared-v2/`), organized by SRS section, with an explicit not-verifiable-here boundary list. |
| Status | Draft |
| Author | Youngho Kim |
| Milestone | Unreleased (post v1.0.2) |
| Related docs | [PRD](PRD.md) · [MRD](MRD.md) · [SRS](SRS.md) · [DESIGN](DESIGN.md) |

## History

| Version | Date | Author | Description |
|---|---|---|---|
| 1.0 | 2026-08-28 | Youngho Kim | Initial TC. |
| 1.1 | 2026-08-28 | Youngho Kim | TC-18: added an explicit assertion that the fitted timeline window contains the returned items, and moved the mock server's fixture timeline data off "today" so this is actually exercised — see DESIGN.md's `updateTimeline()` deviation. |
| 1.2 | 2026-08-28 | Youngho Kim | Corrected `tools/mock-sunapi-server/`'s `getTimeline` fixture to the real `{TimeLineSearchResults: [...]}` envelope (was unwrapped, matching a since-fixed bug in `src/shared-v2/playback.ts` — TC-15/16/18 passed against the wrong shape on both sides without ever exercising the real one) and scaled it to ~150 items to match a real-device report's volume. |
| 1.3 | 2026-08-28 | Youngho Kim | TC-18 rewritten from "new-page-only deviation" back to a real cross-page equivalence check — the "deviation" it was asserting (v1.1) was a misdiagnosis; testing at the new ~150-item fixture volume (v1.2) is what caught both the real envelope bug and the bogus `fit()` "fix" live. |
| 1.4 | 2026-08-28 | Youngho Kim | Added TC-27 — a real-device performance report led to CPU profiling that ruled out vis.Timeline rendering (confirmed <50ms, identical both pages) and live request-counting that found redundant `initSunapiManager()` chains instead; this test asserts the fix (FR-15.5). |
| 1.5 | 2026-08-28 | Youngho Kim | Added TC-28..TC-32 for FR-7.8 (SUNAPI-driven Calendar search) — new-page-only cases, since this feature has no `src/shared/` equivalent to compare against. |
| 1.6 | 2026-08-28 | Youngho Kim | Adjusted TC-15/16/18's shared setup and TC-27's trigger: FR-7.8 hides `#playback_control` whenever SUNAPI is On *and* Playback is selected — the exact state these pre-existing FR-7.1-FR-7.7 tests used to reach it, since realistically that's when a user would want those controls too. They now leave `#use_sunapi_client_checkbox` unchecked and force the SUNAPI session via `#search_overlapped_id`'s/`#hostname`'s own self-init guards instead — still exercises the identical `initSunapiManager()` chain, just without touching the one checkbox FR-7.8 also watches. Found by running the suite, not anticipated during FR-7.8's design. |
| 1.7 | 2026-08-28 | Youngho Kim | TC-30 rewritten (Rule dropdown is now channel-filtered `getDynamicRules()` entries, not a `dynamicrulesoptions`+`dynamicrules` `Type` merge — see SRS.md FR-7.8.2 v1.8) and TC-32 extended to assert `getTimeline`'s request carries `Type=Rule<N>`. Both corrections came from the user reporting real-device behavior directly. |
| 1.8 | 2026-08-28 | Youngho Kim | TC-30's expected values corrected to the 1-based `Rule<N>` numbering (`Rule1`/`Rule2` for CH1, `Rule3`/`Rule4`/`Rule5` for CH2, not `Rule0`-based) — see SRS.md FR-7.8.2 v1.9. |
| 1.9 | 2026-08-28 | Youngho Kim | TC-30 updated for the new `"All"` default option; TC-32 now explicitly selects `Rule1` first (its own Type=Rule<N> assertion would otherwise be untested, since `"All"` is the new default); added TC-33 for the `"All"` default's own `Type=All` query. See SRS.md FR-7.8.2 v1.10. |
| 1.10 | 2026-08-28 | Youngho Kim | Flagged TC-18 as only asserting `.vis-item` count, not position — currently masking a real, still-open item-positioning bug found by the user against real data. See SRS.md FR-7.6 v1.14, DESIGN.md, and MEMORY.md. |
| 1.11 | 2026-08-31 | Youngho Kim | TC-18: the new page's total `.vis-item` count is no longer expected to equal the old page's — FR-7.6 v1.15 now adds a second copy of each Rule#-typed item into its own per-Rule row alongside the merged `"All"` row, so the new page legitimately renders more DOM items. Rewrote the assertion to compare the old page's count against just the new page's `"All"`-row item count (first `.vis-foreground > .vis-group`), and separately assert the new page's grand total is strictly greater (the fixture always includes non-Normal events). See SRS.md FR-7.6 v1.15. |
| 1.12 | 2026-08-31 | Youngho Kim | TC-18: the new page no longer uses `vis.Timeline`/`vis-*` classes at all (FR-7.6 v1.16 replaced it with `src/component/event-timeline/`'s custom widget) — selectors for the new page's side of the comparison changed from `.vis-foreground > .vis-group`/`.vis-item` to `.event-timeline-overview-row .event-timeline-item`/`.event-timeline-item`. The old page (still `vis.Timeline`-based) keeps its original `.vis-item` selector unchanged. Added `docs/event-timeline-component/TC.md` for the new component's own (new-page-only) zoom/pan/select/Hide test cases, not duplicated here. |
| 1.13 | 2026-08-31 | Youngho Kim | Added TC-34: switching `#channel` during an active Calendar-panel Playback session resets/hides `#timeline`, `#calendar_search_area`, Overlapped Id, and Manual Start/End Time, stops the player, and re-fetches the month search for the new channel without re-revealing the search area. Reported directly by the user with a complete 5-item scenario. See SRS.md FR-7.8.4 v1.22. |
| 1.14 | 2026-08-31 | Youngho Kim | TC-31 updated: `#event_rules_type` now also hides before the month search resolves and shows alongside Overlapped Id/Manual Start-End Time, since it moved into `#calendar_search_area`. Added TC-35: a day click right after a channel change (before any month navigation) still correctly reveals `#calendar_search_area`, verifying `onCalendarDayClick()`'s new defensive re-show. Reported directly by the user. See SRS.md FR-7.8.2/FR-7.8.5 v1.23. |
| 1.15 | 2026-08-31 | Youngho Kim | Added TC-36: changing `#event_rules_type` after a day has been clicked immediately re-fetches `getTimeline()` with the new `Type` (same date range/Overlapped Id) and redraws `#timeline`, without re-fetching Overlapped Id; changing it before any day click is a no-op. Reported directly by the user with the real `recording.cgi?msubmenu=timeline` request that should reflect the newly-selected Rule. See SRS.md FR-7.8.2 v1.24. |
| 1.16 | 2026-08-31 | Youngho Kim | Added TC-37: a Timeline result for a Rule configured on a different channel than the one queried is filtered out of `#timeline` entirely, not just mislabeled; an unknown Rule (no matching `getDynamicRules` entry at all) is still kept. Reported directly by the user with a screenshot of CH2's Rule events appearing under CH1. See SRS.md FR-7.6 v1.25. |
| 2.0 | 2026-08-31 | Youngho Kim | v2.0 Playback search redesign: rewrote TC-15/16/17 as new-page-only DEVIATION tests (default "1 day ending now" auto-search, preset-button re-fetch, Selected Time's "Has End Time" checkbox) since the old page's typed-date-range search UI has no equivalent on the new page any more; TC-18 updated to reach its realistic item volume via the new page's "1Y" preset instead of "Search Timeline". TC-31/32/34/36 updated for `currentCalendarSearchRange` replacing `#calendar_start_date`/etc. and Selected Time no longer being set by a day click. Added TC-38: an Event Timeline preset click while the Calendar panel is active re-fetches through the same Overlapped Id + Timeline sequence as a day click, for `[now-preset, now]`. Reported directly by the user; see SRS.md v2.0/DESIGN.md v2.0. |
| 2.1 | 2026-09-01 | Youngho Kim | Added TC-39 for FR-3.4 (SRS.md v2.13): the new page's `#password` show/hide toggle. New-page-only DEVIATION, no `src/shared/` equivalent. Requested directly by the user. |
| 2.2 | 2026-09-01 | Youngho Kim | TC-15/31/32/34 updated for FR-15 (SRS.md v2.14, `docs/event-timeline-component/SRS.md` FR-15 v2.11): Overlapped Id assertions retargeted from `#overlapped_id_area`/`#calendar_overlapped_id_area` to `#timeline .event-timeline-overlapped-id`/`state.eventTimeline?.setOverlappedIds([])`. Requested directly by the user. |
| 2.3 | 2026-09-01 | Youngho Kim | Added TC-40 for FR-7.7.1 (SRS.md v2.19): `#iso_date_time_checkbox` now writes `player.useIsoTimeFormat` for real (New-page-only DEVIATION, removed from § Known dead controls) — fixes a real camera playback seek bug found via a live console trace the user requested. See DESIGN.md's "Deviations from legacy behavior" and `MEMORY.md`. |
| 2.4 | 2026-09-01 | Youngho Kim | Added TC-41 for FR-7.6 (SRS.md v2.24): double-clicking an event directly during active playback now seeks onto that event's own real start and updates Selected Time to match, instead of an approximate pixel position with Selected Time left stale. Reported directly by the user with a console trace. See `docs/event-timeline-component/TC.md` TC-20 for the underlying widget-level test. |
| 2.5 | 2026-09-01 | Youngho Kim | Added TC-42 for FR-6.11 (SRS.md v2.25): `#forward`/`#backward` now call the player's already-implemented `.forward()`/`.backward()` frame-stepping (New-page-only DEVIATION, removed from § Known dead controls) — works independent of Pause/Resume `readyState`, per the user's explicit request. Reported directly by the user. See DESIGN.md's "Deviations from legacy behavior". |

## Method

Every test case (except where noted) loads **both** the original page (`dist/nodejs/examples/
public/`) and the new page (`dist/shared-v2-preview/`) in separate Playwright contexts, performs the
identical action on each, and asserts identical resulting DOM state (value/checked/disabled/
visibility/text content of every element the SRS's corresponding FR names). "Data source" says
which of the two backing fakes a case needs:

- **Synthetic discovery** — a fake `discover` `CustomEvent` dispatched via `page.evaluate()`,
  exactly mirroring what `socket.ts` would dispatch on a real UDP reply. No SUNAPI/mock server
  needed — covers FR-2 (table/topology) entirely.
- **Mock SUNAPI** — the device fields point at `tools/mock-sunapi-server/`'s address; exercises the
  full FR-4.5/FR-5/FR-7 chains end to end.
- **Neither** — pure client-side logic (FR-1, FR-3, FR-6/8/9/10/11 button-state wiring, FR-12/13),
  no backing data needed at all.

| ID | SRS FR | Data source | Steps | Expected result |
|---|---|---|---|---|
| TC-1 | FR-1.1 | Neither | Click `#init`, then `#disconnect` | `#init`/`#disconnect` disabled state flips both times, identically on both pages. |
| TC-2 | FR-1.2 | Neither | Toggle `#auto_discovery_toggle`, reload | Persisted setting round-trips via `/settings`; Start/Stop disabled state matches on reload, identically on both pages. |
| TC-3 | FR-1.3 | Neither | Click the dark-mode switch | `data-theme` and the icon/label flip identically on both pages. |
| TC-4 | FR-2.1/FR-2.2/FR-2.4 | Synthetic discovery | Dispatch 5+ fake devices, type a search substring, click a column header twice | Filtered/sorted row sets and `#datatable_info` text match exactly between pages. |
| TC-5 | FR-2.3 (+ `docs/star-topology/TC.md`'s own cases, re-run here for equivalence) | Synthetic discovery | Switch to Star Topology, group by each of the 5 options, search | Node/edge sets and grouping match exactly between pages — this subsumes `docs/star-topology/TC.md`'s TC-1–TC-11 as equivalence checks, not fresh functional checks. |
| TC-6 | FR-2.5 (+ `docs/control-panel-data-binding.md` §1) | Synthetic discovery | Click a discovered row | Every field §1's table lists ends in the identical state on both pages. |
| TC-7 | FR-3.1 (+ `docs/control-panel-data-binding.md` §2) | Neither | Change `#player_list` | Every field §2's table lists matches, including the documented always-true-guard behavior. |
| TC-8 | FR-3.2/FR-3.3 | Neither | Change username/password/statistics | Player property writes match. |
| TC-9 | FR-4.1/FR-4.2/FR-4.4 | Neither | Change device type/hostname/port/profile | Player property writes match. |
| TC-10 | FR-4.5 (+ `docs/control-panel-data-binding.md` §3) | Mock SUNAPI | Fill in the mock server's address, turn SUNAPI on | Every field §3's tables list (attributes sync, capability gating, channel populate, timezone/date sync, final button state) matches, including the Turning-Off revert table. |
| TC-11 | FR-4.6 | Mock SUNAPI | Toggle `#use_gmt`/`#timezone`/`#universaltime_checkbox` before and after SUNAPI on | Matches, including the capability-driven disable. |
| TC-12 | FR-4.7 | Neither | Click HTTP/HTTPS switch | `#port` default and player `.https` both match. |
| TC-13 | FR-5.2/FR-5.3 (+ `docs/control-panel-data-binding.md` §4) | Mock SUNAPI | With SUNAPI on, click a profile row | Badge rendering and the documented click-vs-change-event gap both match identically (i.e. the new page reproduces the *same* gap, not a fixed version — see DESIGN.md's deviation list for the one gap that is *not* reproduced). |
| TC-14 | FR-6.1–FR-6.9 | Neither (state-machine only — see boundary list) | Dispatch synthetic `statechange` events (`PLAYING`/`STOPPED`/`PAUSED`/`STEP`) via `page.evaluate()` on the `<rtsp-over-websocket>` element, without a real stream | Every button's disabled state after each synthetic event matches between pages — this is the practical substitute for real playback verification (see boundary list). |
| TC-15 | FR-7.1 (v2.0) | Mock SUNAPI | Enter Playback on the new page | New page only (DEVIATION, `docs/window-ui/DESIGN.md`'s "v2.0 Playback search redesign"): a default "1 day ending now" search auto-fires with no button click — `#timeline .event-timeline-overlapped-id` populates (as of FR-15, `docs/event-timeline-component/SRS.md` v2.11 — was `#overlapped_id_area`) and `#timeline` renders. The old page's own `#search_overlapped_id`/`#search_date` click-driven flow is untouched and unaffected, asserted separately (not cross-page compared any more — the UIs have diverged too far for a single-assertion equivalence). |
| TC-16 | FR-7.1/FR-7.3 (v2.0) | Mock SUNAPI | Click an Event Timeline preset button (e.g. 6H) on the new page | New page only (DEVIATION): a fresh `recording.cgi?msubmenu=timeline` request fires — real re-fetch, not a local re-zoom — superseding the old page's own 1 Day/3 Month toggle + "Search Timeline" button (untouched, unaffected, no new-page equivalent any more). |
| TC-17 | FR-7.4 (v2.0) | Neither | Toggle the new page's `#selected_has_end_time` (inside the Event Timeline widget) | New page only (DEVIATION): unchecking sets the player's `endTime` to `null` (open-ended), re-checking restores a non-null value — replaces the retired `#support_end_time`/`#end_date` pair, which the old page still has untouched. |
| TC-18 | FR-7.6 | Mock SUNAPI | Run a timeline search against a realistic (~150-item) result set on both pages (old: "3 Month" button, unaffected by v2.0; new: the Event Timeline's own "1Y" preset, since FR-7.1-7.3's old search buttons no longer exist there — see TC-15/16 above), click/select an item | Timeline row/item rendering matches by count (see below); item selection populates the new page's own Selected Time (`docs/event-timeline-component/SRS.md` FR-13), not cross-page compared (the old page's Manual Time fields are a different, untouched UI as of v2.0). This is the test that caught two real bugs live: `src/shared-v2/`'s missing `.TimeLineSearchResults` envelope-unwrap (rendered nothing against real data) and, after that fix, a since-removed `fit()` call that broke rendering at this item count even though it looked correct against a 3-item fixture — see DESIGN.md's "Deviations from legacy behavior" (retracted entry) for both. **Since FR-7.6 v1.15** the new page's grand total item count is intentionally *not* compared against the old page's — Rule#-typed events are duplicated into their own per-Rule row alongside the merged `"All"` row, so the new page always renders more DOM items when any Rule#/non-Normal events exist. Instead this asserts the old page's `.vis-item` total equals just the new page's overview-row item count (`.event-timeline-overview-row .event-timeline-item` — an apples-to-apples "every result renders exactly once in the merged row" check), and separately that the new page's grand total (`.event-timeline-item` anywhere) exceeds that (the fixture always includes non-Normal events, so at least one extra per-Rule row item is expected). **Since FR-7.6 v1.16** the new page's selectors are `.event-timeline-*` (its own custom widget), not `.vis-*` — the old page (unaffected, still `vis.Timeline`-based) keeps `.vis-item`. Also since v1.16, the old still-open `vis.Timeline` item-positioning bug (below) no longer applies to the new page at all, since it no longer uses `vis.Timeline` for this feature — see `docs/event-timeline-component/TC.md` for that component's own new-page-only zoom/pan/select/Hide test cases. **Known coverage gap (old page only, as of v1.16)**: the old page's `.vis-item` count assertion still only checks item *count*, never actual on-screen position — the real, still-open positioning bug (every item collapsing to the same pixel position with no inline `left`/`top` at all) remains undetected there; see `DESIGN.md`'s "A separate, still-open bug" note and `MEMORY.md` for the investigation. |
| TC-19 | FR-8.1–FR-8.4 | Neither (state-machine only) | Dispatch synthetic `changemute`/`changevolume` events | Disabled-state and value sync matches. |
| TC-20 | FR-9.1 | Neither | Toggle `#backup_checkbox` with/without a filename, with/without Playback mode | Validation alert and player property writes match. |
| TC-21 | FR-10.1 | Neither | Toggle `#instantplayback_checkbox` | Player `.playType` write matches. |
| TC-22 | FR-11.1 | Neither | Click `#fullscreen` | Player `.fullscreen` flip matches (including the double-toggle-from-the-player's-own-event behavior, reproduced as-is). |
| TC-23 | FR-12.2/FR-12.3 (+ `docs/disclosure-component/TC.md`, re-run for equivalence) | Neither | Toggle Use Debug, trigger a log line, Clear | `#debug` content and the panels' collapse/expand behavior matches. |
| TC-24 | FR-13.1 | Neither | Trigger a popup, click its close button | Both modals hide identically on both pages. |
| TC-25 | § Known dead controls | Neither | Click every control in SRS's dead-controls table on both pages | No error surfaces to the user on either page (console `ReferenceError` on the *original* for the `onclick="..."` ones is expected and accepted — see DESIGN.md; the new page has no listener at all, so nothing fires, which is the equivalence bar per PRD.md's Non-Goals). |
| TC-26 | DESIGN.md's `element.device` deviation | Mock SUNAPI, device type NVR | Run the `getDateInfo()` NVR branch | New page uses `getSelectedPlayer().device`, not a stray loop variable — documented as an intentional deviation, not a failure, in this test's own assertion message. |
| TC-27 | FR-15.5 (DESIGN.md's `sunapiInitInFlight` deviation) | Mock SUNAPI | Fill credentials, click "Use SUNAPI", immediately click Search Date (no wait) | Exactly one `attributes.cgi` request on the new page; the original genuinely sends two (a native blur-triggered `change` on `#password` re-triggers `initSunapiManager()`) — asserted as a deliberate asymmetry, not equality. |
| TC-28 | FR-7.8 (panel switch) | Mock SUNAPI | Select Playback, then toggle SUNAPI On/Off | New page only (no old-page comparison — FR-7.8 doesn't exist on `src/shared/`): `#playback_control_calendar` shows and `#playback_control` hides when both conditions hold; toggling either one back reverts to `#playback_control`, with FR-7.1–FR-7.7's own ids/state left exactly as they'd be without this feature. |
| TC-29 | FR-7.8.1 (Language) | Mock SUNAPI | Turn SUNAPI on while in Playback mode | New page only: `#event_rules_language` has all 16 static options and its selection matches the mock `getDeviceInfo()` response's `Language` field. |
| TC-30 | FR-7.8.2 (Rule dropdown) | Mock SUNAPI | Same as TC-29 | New page only: `#event_rules_type`'s first option is always `{value: 'All', label: 'All'}`, followed by the mock `getDynamicRules()` fixture's `Rules` entries whose `EventSources[].Channel` matches the selected channel (default `#channel`="1" → CH1 → Rule 0/1 only), value `'Rule' + (Rule + 1)` (i.e. `Rule1`/`Rule2` for CH1's Rule 0/1 — the Timeline endpoint's numbering is 1-based), label `RuleName`. Switching `#channel` to "2" re-fetches `dynamicrules` and re-filters to CH2's Rule 2/3/4 (i.e. `Rule3`/`Rule4`/`Rule5`, `"All"` still first), replacing (not appending to) CH1's options. |
| TC-31 | FR-7.8.3/FR-7.8.4 (Calendar + month search) | Mock SUNAPI | Same as TC-29, then wait for the initial month search | New page only: `getCalendarSearch` is called for the current month/selected channel; the Calendar renders the current month with the mock fixture's recorded days visibly distinguished (`.calendar-day-has-recording` present on those day cells, absent on others); `#calendar_search_area` (containing `#event_rules_type`, Overlapped Id no longer among its children as of FR-15) becomes visible after the search resolves. **As of v1.13**, `#event_rules_type` is also hidden before this search resolves (it now lives inside `#calendar_search_area`, not its own field-row) and becomes visible at the same moment — asserted via `#calendar_search_area`'s own visibility per TC-28's existing pattern (its *options* are still populated independently, per TC-30, regardless of visibility). **As of v2.0**, Manual Start/End Time is no longer part of this reveal — it moved into the shared Event Timeline widget as Selected Time, populated only by an item click (TC-32/TC-18), not by this month search. |
| TC-32 | FR-7.8.5 (Day click) | Mock SUNAPI | Select a specific Rule (`Rule1`), then click a highlighted day | New page only: `getOverlappedIdList`/`getTimeline` are both called with that day's `00:00:00`-`23:59:59` range (tracked internally as of v2.0, not written into any DOM field), `getTimeline`'s request URL matching `Type=Rule\d+` (the selected Rule dropdown option's value, e.g. `Rule1` — not a raw `EventSources[].Type` string, and not the un-offset `getDynamicRules()` `Rule` field either); `#timeline .event-timeline-overlapped-id` is populated (as of FR-15 — was `#calendar_overlapped_id_area`) and `#timeline` renders the same way TC-18 already verified `updateTimeline()` does. Selected Time (`docs/event-timeline-component/SRS.md` FR-13) stays at its default until an item is actually clicked — a day click alone does not populate it. |
| TC-33 | FR-7.8.2/FR-7.8.5 (`"All"` default) | Mock SUNAPI | Same as TC-29, then click a highlighted day without changing `#event_rules_type` | New page only: `#event_rules_type`'s value is `"All"` by default; clicking a day with it still selected sends `getTimeline`'s request with `Type=All` — the exact query the user specified (`...&Type=All`). |
| TC-34 | FR-7.8.4 (Channel change during Playback) | Mock SUNAPI | Same as TC-32 (day clicked, `#timeline`/`#calendar_search_area`/Overlapped Id all populated and visible, player started), then switch `#channel` to a different channel | New page only: `#timeline` and `#calendar_search_area` both hide (`display:none`) again (which also hides `#event_rules_type`, now a child of `#calendar_search_area` per TC-31 v1.13); the shared Event Timeline widget's Overlapped Id select is cleared (as of FR-15, `state.eventTimeline?.setOverlappedIds([])` — was `#calendar_overlapped_id`/`#calendar_overlapped_id_span` removal); `currentCalendarSearchRange` clears (v2.0 — was `#calendar_start_date`/etc. before FR-7.4's move into Selected Time); the Calendar's selected-day highlight clears; Selected Time resets to its "now, no end" default (v2.0, `clearSelectedTime()`); the player's `stop()` is called (assert via a spy/event, not just state); a fresh `getCalendarSearch` fires for the new channel and the currently-displayed month (highlighted days update to the new channel's fixture data) without `#calendar_search_area` re-appearing on its own. A second variant repeats this with Play Type switched to Live first (SUNAPI still On): `#timeline` hides and the player stops, but none of the Calendar-panel-specific resets/re-fetches run (the panel itself is already hidden by FR-7.8's own visibility switch, not this handler). |
| TC-35 | FR-7.8.5 (Day click right after a channel change) | Mock SUNAPI | Same as TC-34's channel-change step (leaves `#calendar_search_area` hidden, no month navigation in between), then click a highlighted day for the new channel without navigating months first | New page only: `#calendar_search_area` (and `#event_rules_type`/Overlapped Id inside it) becomes visible again as part of handling the click itself, before the new Overlapped Id/Timeline results populate it — verifying `onCalendarDayClick()`'s defensive re-show, not just relying on a prior month-navigation to have already revealed it. |
| TC-36 | FR-7.8.2 (Rule change re-fetches Timeline) | Mock SUNAPI | Click a highlighted day with `#event_rules_type` left at `"All"` (so `#timeline` renders the full mock fixture), then change `#event_rules_type` to `"Rule1"` without clicking a new day | New page only: a fresh `getTimeline` request fires with the *same* `FromDate`/`ToDate`/`ChannelIDList`/`OverlappedID` as the day click but `Type=Rule1`; `getOverlappedIdList` is NOT called again (spy call count unchanged from the day click); `#timeline` redraws from the new response (item count/labels reflect the mock fixture's `Rule1`-only subset, not the prior `"All"` set). A second variant changes `#event_rules_type` *before* any day has been clicked (right after the panel opens): no `getTimeline` request fires at all (`currentCalendarSearchRange`'s own `null` no-op guard, v2.0 — was `buildCalendarSearchTimeRange()`'s empty-date check before FR-7.4's move). |
| TC-38 | FR-7.8.2/FR-7.8.5 (Preset click re-fetches Calendar's own Timeline, v2.0) | Mock SUNAPI | Same as TC-32 (day clicked, `#timeline` populated), then click an Event Timeline preset button (e.g. 1W) | New page only: a fresh `getOverlappedIdList`/`getTimeline` sequence fires for `[now-1week, now]` instead of the previously-clicked day's own range — the exact same sequencing/ids as a day click, just a different computed range, verifying `onCalendarRangePresetSelect()` routes through `runOverlappedAndTimelineSearch()`. |
| TC-39 | FR-3.4 (`#password` show/hide toggle) | Neither | On the new page: fill `#password`, click `#password_toggle` twice | New page only (DEVIATION, no `src/shared/` equivalent): `#password` starts `type="password"`. First click sets `type="text"` (value now visible), `aria-pressed="true"`, `aria-label="Hide password"`, and swaps which of the two inline icons is `hidden`; second click reverts all four back to the masked state. Throughout, `#password`'s `.value` and `player.password` (written by its own unrelated `change` handler, FR-3.2) are unaffected by either click. |
| TC-37 | FR-7.6 (channel-filtered Timeline rows) | Mock SUNAPI | Fixture `getTimeline()` response includes a `Rule5` result whose configured `EventSources[].Channel` (per the `getDynamicRules` fixture) is Channel 2, while `#channel` is "1" (CH1) | New page only: no `Rule5` row/item appears anywhere in the rendered `#timeline` (neither the merged `"All"` row nor its own per-Rule row) — filtered by `eventAppliesToChannel()` before `updateTimeline()` builds anything, not just mislabeled. A second variant includes a `Rule<N>` Type with no corresponding entry in the `getDynamicRules` fixture at all: it IS still rendered (using the existing unresolved-label fallback), confirming "unknown" is kept, not filtered. |
| TC-40 | FR-7.7.1 (`#iso_date_time_checkbox` wired) | Neither | On the new page, with a `<rtsp-over-websocket>` element selected: toggle `#iso_date_time_checkbox` checked, then unchecked | New page only (DEVIATION, no `src/shared/` equivalent — that page's checkbox stays a dead control): checking it sets `getSelectedPlayer().useIsoTimeFormat === true`; unchecking sets it back to `false`. Starts unchecked on page load (matches legacy markup, no `checked` attribute) — `useIsoTimeFormat` is `null`/unset until the checkbox is touched at least once, not defaulted `true` automatically. |
| TC-41 | FR-7.6 (double-click an event during active playback, v2.24) | Neither (synthetic `statechange`/event data, no real RTSP needed) | Start playback (synthetic `PLAYING` `statechange`), then double-click directly inside an Event Timeline item's own bar (not empty track space) whose `start`/`end` differ from the player's current `startTime`/`endTime` | `player.startTime`/`endTime` and Selected Time (`#selected_start_date`/etc.) update to that item's own range — not left on whatever range was set before the double-click, which is what `onSelect`'s own `readyState === PLAYING` skip previously caused. `player.seekingTime` is computed from the item's own `start`, not an approximate pixel position. |
| TC-42 | FR-6.11 (`#forward`/`#backward` wired, v2.25) | Neither | On the new page, with a `<rtsp-over-websocket>` element selected and `playType` set to `PLAYBACK`: click `#forward`, then `#backward`, both while the player is in a `PAUSED` (not `PLAYING`) `statechange` state | New page only (DEVIATION, no `src/shared/` equivalent — that page's buttons stay dead controls, disabled-state toggling only): `getSelectedPlayer().forward()`/`.backward()` are each called once per click, succeeding despite `readyState !== PLAYING` (the library methods only require `playType === PLAYBACK`, not a specific `readyState`). A second variant with `playType` left at `LIVE` asserts the resulting thrown `RTSPOverWebSocketError` is caught and logged (`console.error`), not surfaced to the user — same pattern as TC-25's dead-control click-through, now exercised against a real handler instead of a no-op. |

## Not verifiable in this environment (documented boundary, not a silent gap)

- **Real RTSP video frame rendering.** No real camera/stream is reachable (`CLAUDE.md`'s WSL2
  networking note); TC-14/TC-19 substitute synthetic player events to verify `window.ts`'s *own*
  state-machine logic, which is the part actually within this SDD's scope — actual video decode/
  render is the vendored player's concern, already out of scope per [PRD.md](PRD.md)'s Non-Goals.
- **Chrome-extension-only paths** (`IS_EXTENSION` branches: native TLS proxy, `chrome.storage`-backed
  auto-discovery persistence, extension-to-extension forwarding, known-devices catch-up). Structurally
  implemented in `src/shared-v2/` for spec completeness, not exercised by Playwright (nodejs target
  only) — see [PRD.md](PRD.md)'s Non-Goals.
- **Real Digest authentication** against actual camera firmware — the mock server returns `200`
  directly rather than modeling a `401` challenge/response round (that logic lives in the native
  host, itself Chrome-extension-only and separately spec'd in `docs/native-https-proxy/`). This
  also means a real, confirmed race in the vendored `@melchi45/rtsp-over-websocket` library's
  digest-auth retry counter (shared, unscoped `authCount` state — see `MEMORY.md`) — which
  intermittently 401'd `playbackCalendar.ts`'s `getCalendarSearch()` against a real device when it
  raced `getDeviceInfo()`'s own cold challenge — cannot be reproduced or regression-tested here;
  the mitigation (`runMonthSearch()`'s `firstShowBarrier`) is verified by code review and against
  the real device only, not by an automated test in this suite.
